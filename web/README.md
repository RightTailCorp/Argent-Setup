# Argent Setup UI (web)

Browser prototype of the modernized Argent Job Scheduler Setup wizard.

## Local

```bash
cd web
npm install
npm run dev
```

Open http://localhost:3000

## Deploy

```bash
cd web
npx vercel --prod
```
